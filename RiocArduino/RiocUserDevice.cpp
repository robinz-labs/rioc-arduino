#include "RiocUserDevice.h"
#include "ROUserChannel.h"

// called in setup()
void initUserDevice()
{

}

// called in loop()
void processUserDevice()
{
    /*
    for (int n=0 ; n<CHANNEL_COUNT ; n++) {
        ROUserChannel* channel = (ROUserChannel*)channelObject[n];
        if (channel != NULL) {
            channel->write(random(10000000));
        }
    }
    */
}
